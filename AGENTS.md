# AGENTS.md · 拼豆项目核心手册

> 这是项目的**唯一入口文档**，给「我自己」和「AI 代理」（Kiro / Cursor / Claude Code 等）共同使用。  
> 单人团队不需要多份协作文档，所有跨阶段不变的规则、约定、上下文都集中在这里。

---

## 0. 项目一句话说明

拼豆（Perler Beads）小程序：把用户照片自动转化为可购买的拼豆图纸 + 精确算料 + 一键下单的 C2M 平台，含微信/抖音双端 + 未来 IoT 智能板 + B 端 SaaS。

**项目性质**：单人独立开发 + AI 代理协作（不是团队项目）

---

## 1. AI 代理使用本仓库的规则 ⭐

> 这一节是给 AI 看的，**任何代理在做实质性变更前，先读完这一节**。

### 1.1 必读文档（按优先级）

每次开始新任务，至少读完前 4 份：

| # | 文档 | 用途 |
|---|---|---|
| 1 | [`/guihua.md`](./guihua.md) | 战略与商业判断（Why） |
| 2 | [`/project-plan.md`](./project-plan.md) | 研发计划（What/When） |
| 3 | [`/project-flow-and-milestones.md`](./project-flow-and-milestones.md) | 流程与里程碑（How） |
| 4 | [`/decision-log.md`](./decision-log.md) | **关键决策记录（最重要）** |
| 5 | [`/docs/README.md`](./docs/README.md) | 详细文档索引（按需查） |

### 1.2 改文档的规则

| 操作 | 规则 |
|---|---|
| 新增文档 | 顶部加 metadata（见 §1.3），底部加变更日志 |
| 修改 ADR-001~010 决策 | ❌ 禁止静默改，必须新建 ADR-XXX 取代 |
| 改 PRD / 架构 / 数据模型 | 在文档底部"变更日志"加一行 |
| 删除内容 | ❌ 禁止直接删，标 ⛔ + 注明取代方案 |

### 1.3 新文档的 metadata 模板（精简版）

```yaml
文档名: <名称>
版本: v0.1
最后更新: YYYY-MM-DD
关联文档: <列出依赖>
关联 ADR: <如有>
```

> 单人项目不需要 Owner / Reviewers / 复盘节奏，按需复盘。

### 1.4 改代码的规则（Phase 1 启动后适用）

- 遵循 [`/docs/11-coding-standards.md`](./docs/11-coding-standards.md)（待创建）
- 提交信息用 Conventional Commits：`feat: ...` / `fix: ...` / `docs: ...` / `refactor: ...`
- **不要**自作主张引入 `/project-plan.md` 技术栈以外的新依赖，先讨论
- **不要**把代码与文档变更混在同一个 commit

#### Git 工作流（单人项目精简版）

当前是单人项目，**不走 PR / Code Review 流程**：

- ✅ **直接在 main 分支开发并提交**，省掉分支管理开销
- ✅ 重大变更（架构调整 / 新功能上线）打 `git tag v*.*.*`，方便回滚
- ✅ 每次 commit 都遵循 Conventional Commits 规范，让 git log 自带文档价值
- ✅ 推送前先用 `git diff` 自查一遍，单人项目这就是「自我 Code Review」
- ⏳ **未来扩展**：当团队 ≥ 2 人时，再切换到 PR 工作流并补 `/docs/11-coding-standards.md` 中的分支策略（届时立 ADR 记录变更原因）

### 1.5 ⭐ 大改动强制提交规则（单人项目版本兜底）

> 单人项目没有 Code Review 兜底，**唯一的安全网就是 git 历史**。  
> 一个失误的本地改动如果没及时提交，可能让几小时的工作不可恢复。

**硬性规则**：

| 变更规模 | 处置 |
|---|---|
| 单次任务变更 ≤ 200 行 | 完成后正常提交即可 |
| 单次任务变更 **> 200 行** | ⚠️ **任务完成后必须立即提交到仓库**，不允许继续做下一个任务前积压未提交的代码 |
| 单次任务变更 > 500 行 | 强烈建议**拆成多个 commit**（按逻辑分组），每个 commit 独立可回滚 |

**怎么算"行数"**：
- 使用 `git diff --stat` 输出的 `insertions + deletions` 之和
- 不计入：自动生成的 lock 文件、构建产物、迁移生成的 schema 快照

**AI 代理执行此规则的方式**：

1. 任务开始前用 `git status` 确认工作区干净
2. 任务完成时主动跑 `git diff --stat` 检查变更量
3. 若超过 200 行，**主动询问用户是否提交**，并给出建议的 commit message（按 Conventional Commits 规范）
4. 用户确认后立即提交，**不留着继续叠加新任务**

**为什么 200 行**：
- 大约是 1~2 个功能模块的合理粒度
- 超过这个量未提交，单点故障代价过高（断电 / IDE 崩溃 / 误操作）
- 也是未来回看时还能"一眼看懂这次改了啥"的上限

### 1.6 不要做的事

| ❌ 禁止 | ✅ 应该 |
|---|---|
| 凭空修改已有 ADR 决策 | 新增 ADR 取代旧 ADR |
| 创建非必要的 README/教程文档 | 复用已有文档 |
| 跳过决策门强行进入下一 Phase | 等评估后再说 |
| 假装做过没做的事 | 明确说"做不到 / 需要补什么" |
| 在简单任务上叠加无意义流程 | 直接干 |

### 1.7 反馈循环

- 发现文档冲突 → 在响应里标 `[doc-conflict]` + 描述
- 决策不清晰 → 在 [`/decision-log.md`](./decision-log.md) §3 补一条 TBD-XX
- 工具/资源缺失 → 直接说，不要硬编

---

## 2. 项目当前状态

```yaml
当前阶段: M0 启动决策门（Phase 1 启动前）
项目周次: Pre-Week 1
仓库类型: 文档驱动（Docs-First），代码尚未启动
最近重大决策: ADR-001 ~ ADR-010 已建立基线；ADR-011 ~ ADR-026（PRD v0.8 灵魂对齐第二轮）；ADR-027（部署侧整体迁阿里云）；ADR-028（部署形态收敛为单 ECS Docker Compose 全栈，部分取代 ADR-027）
未决议题: TBD-01 ~ TBD-10（详见 decision-log.md §3）
最后更新: 2026-05-18
```

> 完成重大里程碑后，记得更新这一节。

---

## 3. 仓库结构

```
ai-pin-dou/
├── AGENTS.md                          ← 本文件（项目核心手册）
├── guihua.md                          ← L0 战略
├── project-plan.md                    ← L0 研发计划
├── project-flow-and-milestones.md     ← L0 流程与里程碑
├── decision-log.md                    ← L0 决策记录（ADR）
└── docs/                              ← L1+ 详细文档
    ├── README.md                      ← 文档索引
    ├── 01-prd.md                      ← 产品需求
    ├── 03-design-system.md            ← 设计系统
    ├── 04-system-architecture.md      ← 系统架构
    ├── 05-data-model.md               ← 数据模型
    └── 06-api-spec.md                 ← API 契约
```

> 当前阶段仓库**仅有文档，无代码**。代码目录将在 Phase 1 W1 启动时创建。

---

## 4. 项目特殊上下文

### 4.1 业务特点（务必理解）

- **C2M 柔性供应链**：每张图纸都是定制订单，没有标品库存
- **三方协同**：C 端用户 / 创作者 / B 端门店 / 供应链
- **强合规要求**：SGS 认证 / 内容安全 / 高温安全提示
- **多端**：微信 + 抖音 + B 端 PC + 未来 IoT 智能板
- **IP 红线**：MVP 仅做用户个人照片，未授权 IP 一律不做（关联 ADR-001）

### 4.2 命名约定

- 项目对外称 **「拼豆」**，英文项目代号 `pin-dou`
- 用户积分单位：**PX（像素币）**（关联 ADR-007，单一货币双重价值）
- 难度档：`easy` / `normal` / `pro`（不要用 `hard` / `expert`）
- 颜色品牌：MVP 仅 `mard`（关联 ADR-004）

### 4.3 关键术语词汇表

| 术语 | 含义 |
|---|---|
| 算料 | 计算每个色号需要多少颗豆子 |
| 色卡 | 品牌官方色号库 |
| 配料单 | 推送给仓库的精确分拣指令 |
| 代拼 | C2C 撮合：达人代用户拼装成品 |
| 沉浸式向导 | 智能拼豆板配合 LED 灯逐色引导制作 |
| 像素币（PX） | 用户积分，可解锁技能也可兑换实物 |
| Go/No-Go | 里程碑决策门：达标过 / 不达标停 |

---

## 5. 技术栈速查（关联 ADR-002 / ADR-027 / ADR-028）

| 层级 | 选型 |
|---|---|
| 前端 | uni-app + Vue3 + NutUI |
| 后端 | Python 3.11 + FastAPI |
| 算法 | OpenCV + rembg + scikit-learn |
| 数据库 | PostgreSQL 15 + Redis 7（阿里云 RDS + 云数据库 Redis）|
| 对象存储 | 阿里云 OSS（+ CDN）|
| 部署 | **MVP：单台阿里云 ECS + Docker Compose 全栈**（biz-api / algo-api / inv-scanner / logi-subscriber / celery-worker / nginx）；后续按 04-system-architecture.md §4.4 阈值剥离算法到第二台 ECS 或函数计算 FC（关联 ADR-028）|
| 镜像仓库 | 阿里云容器镜像服务 ACR |
| 日志监控 | 阿里云 SLS + Prometheus/Grafana |
| 支付 | 微信支付 + 字节支付 |
| 包管理 | uv（Python） + pnpm（前端） |
| Lint | ruff（Python） + ESLint + Prettier（前端） |
| 测试 | pytest + Vitest |

> 引入新依赖前，先想"是否真的必要 + 维护成本是否可控"。单人项目，依赖越少越好。

---

## 6. 单人开发工作流

### 6.1 三类典型任务

#### 任务 A：增加新功能 / 改产品逻辑
1. 检查 `/docs/01-prd.md` 是否已有用户故事，没有就先补
2. 评估对架构 / 数据模型 / API 的影响
3. 如有重大决策，立 ADR
4. 直接实现 → 自测 → 提交

#### 任务 B：修复 Bug
1. 复现 → 写最小测试用例固化
2. 修复 → 测试通过 → 提交
3. 如果是已知 ADR 决策出问题，**在 ADR 加一条 Changelog**

#### 任务 C：调整架构 / 技术栈
1. **必须先立 ADR**（即使是单人，未来回看时也需要 Why-Changed）
2. ADR 通过后，再修代码 + 文档
3. 在 [`/docs/04-system-architecture.md`](./docs/04-system-architecture.md) 同步变更

### 6.2 单人项目的"评审"

- **不需要别人 Review**，但每次重要变更前问自己 3 个问题：
  - ① 这个改动符合战略吗？（回看 `guihua.md`）
  - ② 是否影响已有 ADR 决策？
  - ③ 半年后的我能看懂这次改动吗？
- 重要变更（新功能 / 架构调整）打 git tag，方便回滚

### 6.3 文档更新节奏（按需，不强制）

- 改完功能：同步更新对应 PRD 用户故事的 AC 状态
- 决策门评估时（M1/M2/M3/M4）：复盘所有 v0.x 文档，能升 v1.0 的就升
- 季度梳理：归档不再相关的 TBD，新增决策

---

## 7. 文档体系（5 层简化版）

> 单人项目不需要分 10 层那么细，按 5 层用就够了：

| 层 | 是什么 | 文件 |
|---|---|---|
| L0 战略 | 为什么 + 做什么 + 何时做 + 决策记录 | `/guihua.md` `/project-plan.md` `/project-flow-and-milestones.md` `/decision-log.md` |
| L1 产品 | PRD（用户故事 + 验收标准） | `/docs/01-prd.md` |
| L2 设计 | 设计系统 + 关键页面线框 | `/docs/03-design-system.md` |
| L3 技术 | 架构 + 数据模型 + API + 算法 | `/docs/04-*` `/docs/05-*` `/docs/06-*` `/docs/07-*` |
| L4 落地 | 编码规范 + 部署 + 埋点 + 合规 | `/docs/11-*` `/docs/13-*` `/docs/16-*` `/docs/22-*` |

> L3 + L4 的具体文档按 [`/docs/README.md`](./docs/README.md) 索引按需创建，不必一开始就齐。

---

## 8. 启动 Checklist（M0 → Phase 1 W1）

> Phase 1 启动前必须完成的事：

### 🔴 阻塞项（这些不过不能进 Phase 1）

- [ ] **算法可行性预实测**（C10 决策）：
  - Jupyter notebook 跑 100 张真实样图（猫/人脸/宠物/风景各 25）
  - 实测 P95 耗时 + 优良率
  - 若 P95 > 10s 或优良率 < 60% → MVP 推迟，先调算法
- [ ] **微信小程序企业主体注册**（C9 决策）：电商类目，含支付资质
- [ ] **至少 1 家 SGS 认证供应商签 MoU**：拿到 SGS 证书图片用于 US-9.2
- [ ] **启动资金到位**：覆盖 6 个月研发 + 供应链押金

### 🟠 文档准备（v0.5+ 即可启动 W1）

- [x] PRD v0.5 落定（含 C1~C11 决策）
- [ ] PRD 升 v1.0（C12 微信小店联动决策落定后）
- [ ] 设计系统：品牌主色 + 7 个核心页面 Figma 完成
- [ ] 系统架构：C4 - L2 容器图
- [ ] 数据模型：DDL 评审通过 + Alembic 初始化
- [ ] API 契约：OpenAPI YAML 源文件就绪
- [ ] 编码规范 + 配套 lint 配置
- [ ] 隐私政策 + 用户协议（小程序审核必需）

### 🟢 冷启动准备（C8 决策）

- [ ] 100 名内测用户名单
- [ ] 5 位以上小红书 KOL 协议
- [ ] 30 张真实案例图（首页 US-0.3 用）

### 🟦 基础设施（关联 ADR-027 / ADR-028）

- [x] **阿里云 ECS 规格已确认**（≥ 2C4G，业务服务承载 MVP < 50 QPS）— 2026-05-18 已完成
- [x] **域名备案完成**（业务服务对外域名）— 2026-05-18 已完成
- [x] **ICP 备案完成**（微信支付回调要求）— 2026-05-18 已完成
- [ ] 阿里云 RDS PostgreSQL 15 实例开通 + 同 VPC 内网放通
- [ ] 阿里云云数据库 Redis 7 实例开通 + 同 VPC 内网放通
- [ ] 阿里云 OSS Bucket 开通 + 生命周期规则（原图 24h 自动删除，关联 04-system-architecture.md §5.2）
- [ ] 阿里云容器镜像服务 ACR 命名空间创建（biz-api / algo-api 镜像存储）
- [ ] 阿里云 SLS 日志项目 + Project/Logstore 创建（容器日志统一采集）
- [ ] ECS 安装 Docker + Docker Compose，编写 `docker-compose.yml` 模板（含 nginx / biz-api / algo-api / inv-scanner / logi-subscriber / celery-worker，关联 ADR-028）
- [ ] ECS ↔ RDS / Redis / OSS 同 VPC 内网连通性自检
- [ ] 阿里云监控 + 企业微信告警接入（特别是 §4.4 回切阈值）

**进度判定**：阻塞项必须 100% 通过；其他完成 ≥ 70% 即可进入 Phase 1。

---

## 9. 紧急情况手册

| 场景 | 怎么办 |
|---|---|
| 算法上线后准确率严重下滑 | 立刻关闭流量，启用上一版镜像，回退算法 |
| 微信小程序被警告 / 下架 | 立刻整改，记录到 `/decision-log.md` 触发 ADR 复盘 |
| 供应链单点故障（Mard 缺货） | 启用 Plan B（同色系替换 + 通知用户） |
| 用户隐私事件 | 立刻禁用相关接口，48 小时内向用户披露 |
| 自己短期不能维护项目 | 在 README 加状态说明，暂停接单与营销投放 |

---

## 10. 变更日志

| 日期 | 版本 | 变更 | 备注 |
|---|---|---|---|
| 2026-05-17 | v0.1 | 初始化 AGENTS.md | 项目启动 |
| 2026-05-17 | v0.2 | 改造为单人项目核心手册（去除团队协作冗余） | — |
| 2026-05-17 | v0.3 | 新增 §1.5 大改动强制提交规则（>200 行必须提交） | 单人项目版本兜底 |
| 2026-05-17 | v0.4 | §1.4 明确单人项目直接提交 main，不走 PR；未来团队 ≥ 2 人时再切换 | — |
| 2026-05-17 | v0.3 | 新增 §1.5 大改动强制提交规则（>200 行必须提交） | 单人项目版本兜底 |
| 2026-05-18 | v0.5 | §2 项目当前状态同步 ADR-011~027；§5 技术栈表"部署"行更新为阿里云 ECS + 函数计算 FC，新增 ACR 与 SLS 行 | 关联 ADR-027 |
| 2026-05-18 | v0.6 | §8 启动 Checklist 新增「🟦 基础设施」小节：ECS 规格 / 域名备案 / ICP 备案 已完成；RDS / Redis / OSS / FC / ACR / SLS 待开通 | 关联 ADR-027 |
| 2026-05-18 | v0.7 | §5 技术栈表"部署"行收敛为单 ECS + Docker Compose 全栈；§2 项目当前状态 + §8 基础设施 checklist 同步去掉 FC 函数骨架开通项，改为 docker-compose 模板编写项 | 关联 ADR-028 |
